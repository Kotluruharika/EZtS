N=int(input())
def array(N):
    digits=0
    for digit in N:
        if digit==0:
            return 0
        elif sum==digit:
            return digit
        elif digit>90:
            return 1
print(N)  
